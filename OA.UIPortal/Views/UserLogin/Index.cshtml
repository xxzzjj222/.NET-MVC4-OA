@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <script src="~/Scripts/jquery-1.8.2.min.js"></script>
    <script src="~/Scripts/jquery.unobtrusive-ajax.js"></script>
    <script>
        function loginSuccess(data) {
            if (data == "succeed") {
                window.location.href = "/UserInfo/Index";
            } else if (data == "failed") {
                alert("登录失败");
            }
        }
    </script>
</head>
<body>
    @using (Ajax.BeginForm("ProcessLogin", "UserLogin", new AjaxOptions() { OnSuccess = "loginSuccess" }))
    {
    <div>
            @Html.Label("同户名")
            @Html.TextBox("UserName")
            @Html.Label("密码")
            @Html.TextBox("Password")
            <input type="submit" value="提交" style="width:50px;height:50px" />
    </div>
    }
</body>
</html>
